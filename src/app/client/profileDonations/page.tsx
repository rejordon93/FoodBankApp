import React from "react";
import { Typography } from "@mui/material";

export default function PrfileDonations() {
  return (
    <div>
      <Typography>Prfile Donates</Typography>
      {/* later update Prfile with fullname */}
    </div>
  );
}
